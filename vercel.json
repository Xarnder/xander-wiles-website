{
    "buildCommand": "cd pages/journal && npm install && npm run build && cp -r dist/* .",
    "outputDirectory": ".",
    "rewrites": [
        {
            "source": "/pages/journal/:path*",
            "destination": "/pages/journal/index.html"
        }
    ]
}