{
    "buildCommand": "cd pages/journal-pwa && npm install && npm run build && cp -r dist/* .",
    "outputDirectory": ".",
    "rewrites": [
        {
            "source": "/pages/journal-pwa/:path*",
            "destination": "/pages/journal-pwa/index.html"
        }
    ]
}