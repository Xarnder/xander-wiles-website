# Optional GLB models

Drop custom files here using these exact lowercase names:

```text
fighter-jet.glb
missile.glb
```

See the project README for scale, axis, material, socket, and node-name
recommendations. Missing or invalid files automatically use procedural models.
